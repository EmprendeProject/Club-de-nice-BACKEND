SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Opcional — habilita caché (analytics) y rate limiting (login/register).
# Si se deja vacío, ambos quedan deshabilitados sin afectar el resto de la app.
REDIS_URL=
